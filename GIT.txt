		Memo git 

git --version -> connaitre version de ton git 

git config --global user.name "(nom)" -> configuration de ton nom
git config --global user.email "(adresse)" -> configuratrion de l'adresse email
mkdir (nom dossier) -> creation d 'un dossier 
git init -> initialisation du git 
git status
git add (fichier) -> selection d'un fichier 
git commit -m "(texte)" -> enregistrement du fichier
git log -> historique de tes modif av descriptions 
git branch -> liste de toutes les branches 
git branch (nom) -> creation d'une branche
git checkout (nom) -> changement de branche 

je proviens d'une branche diff